version: '3.8'

services:
  xscatter:
    build: .
    ports:
      - "8085:8085"
    environment:
      - LRP_SERVICE_URL=http://lrp-service:5001
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    depends_on:
      - lrp-service

  lrp-service:
    build: ./lrp_service
    ports:
      - "5001:5001"
    environment:
      - CKPT_PATH=/model/model.ckpt-200
    volumes:
      - ./lrp_service/model:/model:ro
      - ./static/images:/images:ro
